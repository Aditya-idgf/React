function Chai() {
    const username = "chai here !"
    return(
        <h1>Here he is ! {username}</h1>
    )
    // we use {} to write js in jsx
    // also {} is called a evaluated expression, that means the js inside will be the final outcome
    // all evaluation task is done outside jsx
}

export default Chai;